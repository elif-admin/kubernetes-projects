apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: kittens-carousel-pv-claim
spec:
  storageClassName: manual
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi